#include <stdio.h>

int main() {
    int i, sum = 0;

    for(i = 1; i <= 100; i++) {
        sum = sum + i;
    }

    printf("1 theke 100 porjonto sob sonkhar jogfol = %d\n", sum);

    return 0;
}